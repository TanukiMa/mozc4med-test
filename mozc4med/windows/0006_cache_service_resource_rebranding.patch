Depends-On: 0005_custom_action_resource_rebranding.patch
Touch-Files: src/win32/cache_service/mozc_cache_service.rc
Scope: ui

diff --git a/src/win32/cache_service/mozc_cache_service.rc b/src/win32/cache_service/mozc_cache_service.rc
index 1400e3bb8..907af9de5 100644
--- a/src/win32/cache_service/mozc_cache_service.rc
+++ b/src/win32/cache_service/mozc_cache_service.rc
@@ -44,7 +44,7 @@ LANGUAGE LANG_JAPANESE, SUBLANG_DEFAULT
   #if defined(GOOGLE_JAPANESE_INPUT_BUILD)
     #define MOZC_RES_FILE_DESCRIPTION "Google 日本語入力 キャッシュサービス"
   #elif defined(MOZC_BUILD)
-    #define MOZC_RES_FILE_DESCRIPTION "Mozc Cache Service"
+    #define MOZC_RES_FILE_DESCRIPTION "Mozc4med Cache Service"
   #endif
 #endif
 #ifndef MOZC_RES_FILETYPE
@@ -70,8 +70,8 @@ BEGIN
     IDS_DISPLAYNAME         "Google Japanese Input Cache Service"
     IDS_DESCRIPTION         "Google 日本語入力 の辞書を常に物理メモリに配置し、変換を高速化します。停止しても日本語入力はできますが、パフォーマンスが低下する場合があります。"
 #elif defined(MOZC_BUILD)
-    IDS_DISPLAYNAME         "Mozc Cache Service"
-    IDS_DESCRIPTION         "Mozc の辞書を常に物理メモリに配置し、変換を高速化します。停止しても日本語入力はできますが、パフォーマンスが低下する場合があります。"
+    IDS_DISPLAYNAME         "Mozc4med Cache Service"
+    IDS_DESCRIPTION         "Mozc4med の辞書を常に物理メモリに配置し、変換を高速化します。停止しても日本語入力はできますが、パフォーマンスが低下する場合があります。"
 #endif
 END
 
@@ -81,8 +81,8 @@ BEGIN
     IDS_DISPLAYNAME         "Google Japanese Input Cache Service"
     IDS_DESCRIPTION         "Always allocates conversion dictionary of Google Japanese input into physical memory."
 #elif defined(MOZC_BUILD)
-    IDS_DISPLAYNAME         "Mozc Cache Service"
-    IDS_DESCRIPTION         "Always allocates conversion dictionary of Mozc into physical memory."
+    IDS_DISPLAYNAME         "Mozc4med Cache Service"
+    IDS_DESCRIPTION         "Always allocates conversion dictionary of Mozc4med into physical memory."
 #endif
 END
 
